# v0.4.0 PR Summary

## Summary

This PR finalizes the `v0.4.0` release-prep branch.

- aligns release-facing docs with the shipped `0.4.x` state
- replaces the stale `v0.3.0` handoff with a `v0.4.0` release handoff
- keeps the roadmap/design history in `docs/` as part of the branch
- documents the current PR gate and release boundary: PR-ready now, public publish later

## Test Plan

- [x] `cargo test --workspace` passes
- [x] `cargo clippy --workspace -- -D warnings` clean
- [x] `cargo fmt --all --check` clean
- [x] `cargo package -p cloak-core --allow-dirty --list` shows the expected publishable crate contents

## Checklist

- [x] Tests added/updated for new functionality
- [x] Documentation updated if needed
- [x] `docs/` is intentionally included for release history and PR context
- [ ] Public publish, tagging, and GitHub release notes remain out of scope for this PR
